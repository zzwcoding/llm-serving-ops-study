# 细分路线(learn-by-rebuild 规则执行)

> 执行规则:每阶段小步可验证、有可见变化;用户说"下一步"才推进,说"提交"才 commit;
> 每个新概念讲四要素(名字/作用/参数/用法);每阶段答三问(干嘛的/为什么这么设计/解决什么问题)。
> 讲解正文落盘 lessons/NNNN-*.md,进度记录落盘 learning-records/NNNN-*.md(编号与 lessons 对应)。
> 总目标见 [MISSION.md](./MISSION.md) 与 [学习计划.md](./学习计划.md)。

## 进度总览(2026-08-30 更新)

```
✅ Phase 0 环境准备(0.1~0.4)     ✅ Phase 1 跑起服务(1.1~1.6)
✅ Phase 2 监控(2.1~2.4)         ✅ Phase 3 调参(3.1~3.4)
⬜ Phase 4 压测(4.1~4.3)  ← 下次从这里继续
⬜ Phase 5 量化与 K8s(5.1~5.6)
```

## 前置说明

本机是 Mac mini(无 NVIDIA 显卡),vLLM 必须 CUDA,因此学习全程在租用 GPU 云主机(gpuhome,4090)上进行。
Mac 只作为 SSH 客户端(`ssh gpu` 免密别名)和浏览器入口(SSH 隧道看 Grafana)。

---

## ✅ Phase 0:环境准备

| 步骤 | 内容 | 核心概念 | 教学稿 |
|---|---|---|---|
| 0.1 | 租 4090(24G)实例 | GPU 规格、为什么 vLLM 必须 NVIDIA | lessons/0001 |
| 0.2 | SSH 免密 + `nvidia-smi` | 显存/利用率面板 | lessons/0001 |
| 0.3 | 装 Docker + NVIDIA Container Toolkit | 容器 GPU 透传 | lessons/0002 |
| 0.4 | **阻塞,转实战课**:实例是无特权容器(seccomp 禁 unshare),Docker 不可用 | 环境权限边界诊断三连 | lessons/0002 |

> **调整(2026-08-23):** 租用实例不支持嵌套 Docker。Phase 1 直装不受影响;
> Docker/K8s 概念挪到 Mac 上学;Phase 5 视情况换平台或 minikube。

## ✅ Phase 1:跑起来第一个推理服务

| 步骤 | 内容 | 核心概念 | 教学稿 |
|---|---|---|---|
| 1.1 | uv + venv 装 vLLM,起 Qwen2.5-1.5B | 服务化;HF xet 401 坑(HF_HUB_DISABLE_XET=1) | lessons/0003 |
| 1.2 | 精读启动日志 | 显存预算公式、KV cache、Maximum concurrency | lessons/0004 |
| 1.3 | curl 调 `/v1/chat/completions` | OpenAI 兼容 API;usage/finish_reason | lessons/0005 |
| 1.4 | 边发请求边盯 `nvidia-smi` | 利用率跳、显存恒定(先圈地再营业) | lessons/0006 |
| 1.5 | prefill/decode 概念 + 流式体验 | TTFT/TPOT 定义 | lessons/0007 |
| 1.6 | 恢复演练(实例半重置后用户独立恢复服务) | 数据持久性、授权密钥恢复 | learning-records/0003 |

## ✅ Phase 2:监控与指标

| 步骤 | 内容 | 核心概念 | 教学稿 |
|---|---|---|---|
| 2.1 | 裸看 vLLM `/metrics` | 指标格式;gauge/counter/histogram | lessons/0008 |
| 2.2 | 装 Prometheus(二进制,非 Docker) | pull 抄表模型;`up=1` 验证 | lessons/0009 |
| 2.3 | 装 Grafana + SSH 端口转发 + 首条曲线 | Grafana 只画不存;`ssh -L`;三个配置坑 | lessons/0010 |
| 2.4 | 五面板监控墙 + 30 并发对照实验 | rate()、histogram_quantile()、盯 P99 | lessons/0011 |

产出:`configs/vllm-dashboard.json`(v13 导入被拒,留作改写素材)、`scripts/burst-test.sh`

## ✅ Phase 3:调参实验(每步:改参 → 重启 → 读日志 → 压测 → Grafana 对比)

| 步骤 | 实验 | 数据结论 | 教学稿 |
|---|---|---|---|
| 3.1 | `--max-model-len` 32768→4096 | 并发 20.82x→159.88x;超长请求被 400 拒 | lessons/0012 |
| 3.2 | `--gpu-memory-utilization` 0.92→0.6 | KV 18.2→10.7 GiB,并发 160x→98x 同比缩 | lessons/0013 |
| 3.3 | `--max-num-seqs 8` + 30 并发压测 | running 顶死 8、waiting 抬头、TTFT 含排队时间 | lessons/0014 |
| 3.4 | 前缀缓存 A/B 组对照 | prefix_cache_hits;不变内容放 prompt 最前 | lessons/0015 |

## ⬜ Phase 4:压测(3 步)— 下次从这里继续

| 步骤 | 内容 | 核心概念 |
|---|---|---|
| 4.1 | `vllm bench serve` 单档压测 + 逐行读报告 | bench 用法;报告字段与 Grafana 指标对应 |
| 4.2 | 并发阶梯 2/8/32/64,画吞吐-并发曲线 | 找拐点:吞吐不涨、延迟暴涨的位置 |
| 4.3 | 长输入 vs 短输入对比 | 数据验证"prefill 长度决定 TTFT" |

产出:`notes/04-压测报告.md`——回答"每秒能服务多少用户?超过多少要扩容?"

## ⬜ Phase 5:量化与 K8s 形态(4~6 步)

主题:FP8/AWQ 量化对比(显存/吞吐/质量);production-stack(视平台权限,可能改 Mac minikube 学概念)。
产出:`notes/05-量化对比.md`、`notes/06-k8s形态.md`

---

## 续学指引(跨会话)

回来先读:`learning-records/`(每阶段一条:学了什么/卡在哪/结论)+ `git log`。
服务器现状:gpuhome 实例,服务命令见 learning-records 各条;模型缓存 2.9G 在 `~/.cache/huggingface`;
工具在 `~/work/`(.venv、prometheus、grafana、burst-test.sh、prefix-test.sh)。
