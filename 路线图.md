# 细分路线(learn-by-rebuild 规则执行)

> 执行规则:每阶段小步可验证、有可见变化;用户说"下一步"才推进,说"提交"才 commit;
> 每个新概念讲四要素(名字/作用/参数/用法);每阶段答三问(干嘛的/为什么这么设计/解决什么问题)。
> 总目标见 [学习计划.md](./学习计划.md)。

## 前置说明

本机是 Mac mini(无 NVIDIA 显卡),vLLM 必须 CUDA,因此学习全程在租用 GPU 云主机(AutoDL)上进行。
Mac 只作为 SSH 客户端和浏览器入口。

---

## Phase 0:环境准备

| 步骤 | 内容 | 可观察结果 | 核心概念 |
|---|---|---|---|
| 0.1 | 注册 AutoDL,租 4090(24G),选 PyTorch 官方镜像 | 控制台看到运行中的实例 | GPU 规格(显存/算力)、为什么 vLLM 必须 NVIDIA |
| 0.2 | SSH 登录,跑 `nvidia-smi` | 显卡状态面板 | 显存占用、GPU 利用率——排障第一扇窗 |
| 0.3 | 装 Docker + NVIDIA Container Toolkit | ~~`docker info` 正常~~ | 容器如何透传宿主机 GPU |
| 0.4 | ~~`docker run --gpus all ... nvidia-smi`~~ | **阻塞**:实例是无特权容器(seccomp 禁 unshare),Docker 不可用 | **实战课:如何判断环境权限边界**(见 notes/00 翻车实录) |

> **调整(2026-08-23):** 租用实例不支持嵌套 Docker。Phase 1 直装不受影响;
> Docker/K8s 概念挪到 Mac 上学;Phase 5 视情况换平台或 minikube。

## Phase 1:跑起来第一个推理服务

| 步骤 | 内容 | 可观察结果 | 核心概念 |
|---|---|---|---|
| 1.1 | `pip install vllm`,起 Qwen2.5-1.5B(不用 Docker,先直装降低心智负担) | 模型加载日志 | 服务化:模型文件 → HTTP 服务 |
| 1.2 | 逐行读启动日志 | 看懂日志每个字段 | 显存分配、KV cache 容量、最大并发数 |
| 1.3 | curl 调 `/v1/chat/completions` | 拿到模型回复 | OpenAI 兼容 API 的事实标准地位 |
| 1.4 | 发请求时盯 `nvidia-smi` | 空闲 vs 忙碌的显卡状态对比 | 推理 = 显存占用 + GPU 计算 |
| 1.5 | 概念课:一次请求的两段旅程 | 能复述 prefill/decode 区别 | TTFT、TPOT(先概念,Phase 2 再测量) |
| 1.6 | 收口:笔记进 `notes/01-第一个推理服务.md`,分步 commit | git log 留痕 | 提交习惯 |

## Phase 2:监控与指标(到跟前再细分,预计 4~5 步)

主题:Prometheus + Grafana;读懂 TTFT / TPOT / QPS / KV cache 使用率四条曲线。
产出:`notes/02-监控与指标.md`

## Phase 3:调参实验(预计 4~5 步)

主题:逐个改 `--max-model-len` / `--gpu-memory-utilization` / `--max-num-seqs` / `--enable-prefix-caching`,每改一个看 Grafana。
产出:`notes/03-参数调优.md`

## Phase 4:压测(预计 3~4 步)

主题:`vllm bench serve`,不同并发档位找拐点。
产出:`notes/04-压测报告.md`

## Phase 5:量化与 K8s 形态(预计 4~6 步)

主题:FP8/AWQ 对比;minikube 部署 vllm production-stack。
产出:`notes/05-量化对比.md`、`notes/06-k8s形态.md`

---

## 每步笔记模板(notes/ 下的文件按此写)

```markdown
# 阶段 X.Y:<标题>

## 这一步干嘛的
## 为什么需要这么设计(解决了什么问题)
## 新概念四要素
- 名字:
- 作用:
- 参数:
- 用法:
## 操作记录(命令 + 关键输出)
## 翻车与解决
```
