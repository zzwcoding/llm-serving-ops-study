# LLM 推理层部署运维学习计划

> 定位:**Agent 应用开发 + 推理层部署运维**(全栈 LLM 工程师)
> 目标:学到"能部署、能调参、能排障",不学推理引擎源码开发
> Agent 层(LangGraph 等)已掌握,本计划不含 Agent 编写内容

## 核心能力目标

学完后应该能做到:

1. 用 vLLM/SGLang 在 GPU 机器上部署开源模型,暴露 OpenAI 兼容 API
2. 会调关键参数:`--max-model-len`、`--gpu-memory-utilization`、`--max-num-seqs`、量化选项、前缀缓存
3. 会估算显存:模型权重 + KV cache,判断单卡能装多大模型、开多少并发
4. 会用 Prometheus + Grafana 监控推理服务,看懂 TTFT / TPOT / QPS / KV cache 使用率
5. 会压测:`vllm bench serve`,能根据结果调参优化
6. 懂概念:KV cache、前缀复用、continuous batching、PD 分离、量化取舍

## 四个核心指标(贯穿全程)

| 指标 | 含义 | 影响什么 |
|---|---|---|
| TTFT(首 token 延迟) | prefill 耗时 | 用户体验、长 prompt 成本 |
| TPOT(每 token 延迟) | decode 速度 | 流式输出的流畅度 |
| 吞吐(QPS/tokens/s) | 整体容量 | 服务能扛多少用户 |
| 显存占用 | 权重 + KV cache | 能装什么模型、开多少并发 |

---

## Phase 0:环境准备(0.5 天)

- [ ] 准备 GPU 环境:本地有卡 / 租 AutoDL / 云 GPU(建议至少 24G 显存,如 4090/A5000)
- [ ] 装好:Docker、NVIDIA Container Toolkit、Python 3.10+、minikube 或 k3s
- [ ] 注册 HuggingFace(如需国内镜像,配 `HF_ENDPOINT=https://hf-mirror.com`)

**验收:** `docker run --gpus all nvidia/cuda:12.4.0-base-ubuntu22.04 nvidia-smi` 能看到显卡

---

## Phase 1:单机部署 vLLM + 监控(1~2 天)

任务:

- [ ] 用 vLLM 部署一个小模型(推荐 `Qwen/Qwen2.5-7B-Instruct`,显存不够就换 1.5B)
  ```bash
  docker run --gpus all -p 8000:8000 \
    vllm/vllm-openai:latest \
    --model Qwen/Qwen2.5-7B-Instruct
  ```
- [ ] 用 curl 调通 OpenAI 兼容接口 `/v1/chat/completions`
- [ ] 搭监控:参考 [vLLM 官方 Prometheus/Grafana 示例](https://docs.vllm.ai/en/v0.7.2/getting_started/examples/prometheus_grafana.html)(docker-compose 一键起)
- [ ] 在 Grafana 里找到并看懂:`vllm:time_to_first_token_seconds`、`vllm:time_per_output_token_seconds`、`vllm:gpu_cache_usage_perc`

**验收:** 能看着 Grafana 面板说出"现在服务忙不忙、瓶颈在 prefill 还是 decode"

---

## Phase 2:参数调优与显存估算(2~3 天)

任务:

- [ ] 手动算一遍显存:模型权重(参数量 × 字节数)+ KV cache(用公式或 vLLM 日志里的值)
- [ ] 逐个实验这些参数,记录 Grafana 变化:
  - `--max-model-len`(上下文长度 vs KV cache 占用)
  - `--gpu-memory-utilization`(显存水位)
  - `--max-num-seqs`(最大并发序列数)
  - `--enable-prefix-caching`(前缀缓存,对多轮对话/固定 system prompt 的影响)
- [ ] 制造一次 KV cache 打满的场景,观察排队行为

**产出:** 写一份笔记 `notes/02-参数调优.md`,记录每个参数的作用和调参前后的指标对比

**验收:** 给一个模型 + 一张卡的配置,能估算出"能开多少并发、适合什么场景"

---

## Phase 3:压测(2~3 天)

任务:

- [ ] 学会 `vllm bench serve`(vLLM 镜像自带):
  ```bash
  vllm bench serve --model <模型> --num-prompts 100 --request-rate 4
  ```
- [ ] 做三组对比实验:
  1. 不同并发档位(1 / 4 / 16 / 64)下的 TTFT/TPOT/吞吐曲线
  2. 调 `--max-num-seqs` 前后对比
  3. 开/关前缀缓存,用相同 system prompt 的批量请求对比 TTFT
- [ ] 找到这个部署的"拐点":并发到多少时 TTFT 开始暴涨

**产出:** `notes/03-压测报告.md`,附数据表格和结论

**验收:** 能回答"这个服务每秒能服务多少用户、超过多少要扩容"

---

## Phase 4:量化部署对比(1 天)

任务:

- [ ] 从 HuggingFace 拉一个 FP8 或 AWQ 量化版本的同模型(看 [neuralmagic](https://huggingface.co/neuralmagic) 集合)
- [ ] 同机部署,对比:显存占用、吞吐、回答质量(抽 10 个固定问题人工对比)
- [ ] (可选)用 [llm-compressor](https://github.com/vllm-project/llm-compressor) 自己动手量化一次

**验收:** 能说出量化的取舍:省了多少显存、掉了多少精度、什么场景值得用

---

## Phase 5:K8s 生产形态(3~5 天)

任务:

- [ ] 本机装 minikube/k3s,用 Helm 部署 [vllm-project/production-stack](https://github.com/vllm-project/production-stack)
- [ ] 玩明白:请求路由、多副本扩容、自带的可观测性面板
- [ ] (进阶,有时间再看)[vllm-project/aibrix](https://github.com/vllm-project/aibrix):弹性伸缩、KV 感知调度

**验收:** 理解"生产上推理服务是怎么管的",能讲清楚 production-stack 各组件的作用

---

## 概念阅读清单(穿插在各阶段中读)

按顺序读,只读到"能讲给别人听"的程度,不推导公式:

- [ ] FlashAttention 论文(理解 IO 感知思想即可)
- [ ] vLLM / PagedAttention 论文(显存分页)
- [ ] Orca / continuous batching(调度)
- [ ] Mooncake 或 DistServe(PD 分离的概念)
- [ ] SGLang 文档的 RadixAttention 章节(前缀复用)

## 参考项目清单

| 用途 | 项目 |
|---|---|
| 推理引擎 | [vllm-project/vllm](https://github.com/vllm-project/vllm)、[sgl-project/sglang](https://github.com/sgl-project/sglang) |
| 生产部署参考 | [vllm-project/production-stack](https://github.com/vllm-project/production-stack) |
| K8s 控制面(进阶) | [vllm-project/aibrix](https://github.com/vllm-project/aibrix)、[kserve/kserve](https://github.com/kserve/kserve) |
| KV cache 层(背景) | [LMCache/LMCache](https://github.com/LMCache/LMCache)、[kvcache-ai/Mooncake](https://github.com/kvcache-ai/Mooncake) |
| 量化工具 | [vllm-project/llm-compressor](https://github.com/vllm-project/llm-compressor) |
| GPU 集群管理(运维视角) | [gpustack/gpustack](https://github.com/gpustack/gpustack) |
| 多模态编排(拓展视野) | [meituan-longcat/omni-flow](https://github.com/meituan-longcat/omni-flow) |

## 不需要学的(明确排除)

- FlashAttention / FlashInfer 等 CUDA kernel 实现
- vLLM / SGLang 源码级改造
- omni-flow 这类多模态编排框架的内部实现(能讲清定位即可)

## 目录约定

```
llm-serving-ops-study/
├── 学习计划.md          ← 本文件
├── notes/              ← 各阶段笔记和实验报告
│   ├── 02-参数调优.md
│   └── 03-压测报告.md
├── configs/            ← docker-compose、Grafana 配置等
└── scripts/            ← 压测、部署脚本
```
