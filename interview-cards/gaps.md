# 缺口清单 · 面试答题卡诚实分级

> 主 agent 凭全程记录判断得准,子 agent 不知道这张全局视图。
> 用途:面试前决定"哪些值得补最小实验 / 哪些用理论答 / 哪些一句话带过"。

---

## 数字纪律回顾

- **做过:** 部署 vLLM 跑 Qwen2.5-1.5B(24G 4090)、精读启动日志、四个调参实验、Prometheus+Grafana、30 并发 burst-test
- **没做过:** 训练微调、RAG、Agent 框架自研、CUDA kernel、多卡分布式、生产 K8s、量化模型实跑
- **理论但缺实操:** 多模态、推测解码、PD 分离、production-stack

---

## P0 · 值得补最小实验(成本低、面试高频、收益大)

| 缺口 | 影响题数 | 最小实验成本 | 面试命中 |
|---|---|---|---|
| **Phase 4: `vllm bench serve` 并发阶梯** | T07 全部 14 题,以及 T11 中"线上吞吐"类问题 | **1-2 小时**(工具自带,跑 2/8/32/64 四档) | 直接命中"压测怎么找拐点、能扛多少 QPS、何时扩容" |
| **生产 tokens/s 实测数字** | T11 q0297、q0385、q0582 等"线上吞吐"类 | 复用 Phase 4 数据 | 字节/淘天/腾讯二面三面常问 |
| **PD 分离(Disaggregated Prefill)实操** | T09 q3704、T11 中长 prompt 延迟题 | **2-4 小时**(vLLM 0.27 支持实验性 prefill/decode 拆分) | SGLang 对比题、推理架构设计题 |

**建议:** 在项目里跑完 Phase 4 + 简单 PD 分离 demo,补上后上面三列缺口题从"承认未实践"升级到"实测证据 + 数字"。

---

## P1 · 理论即可(没必要补实验,诚实答原理 + 引回相邻锚点)

| 缺口 | 影响题数 | 安全答法 |
|---|---|---|
| **多卡 TP/PP 切分** | T06(3)、T02(部分)、T11(部分) | "我没实操过多卡,但 PagedAttention 的 block table 设计就是为了将来扩展到多卡,核心难点是跨实例 KV 不共享 → prefix 路由策略" |
| **K8s production-stack** | T06(2)、T11 中"生产部署"题 | "我项目环境是无特权容器(Phase 0 踩坑过 seccomp 禁 unshare),Docker 不可用,production-stack 的 HPA/PDB/服务网格我都只读过文档,没实操" |
| **SGLang 实操** | T01 q4156 等 | "我没用过 SGLang,RadixAttention 思路来自论文 + 文档,如果要选 vLLM vs SGLang 我会按 prefix 复用占比做决策" |
| **量化模型(INT8/FP8/AWQ)部署** | T05 全部 11 题 | "我项目模型是 FP16/BF16 原生,没部署过量化模型;但启动日志里的显存公式可以推算量化能省多少——INT8 KV cache 是 FP16 一半,理论上并发翻倍" |
| **推测解码(Speculative Decoding)** | T04、T11 | "原理懂:小模型草拟 N token,大模型一次 verify 接受/拒绝;但项目里没跑过。" |
| **前端流式聚合(SSE/WebSocket/EventSource)** | T04 大部分 | "我只用 curl `-N` 验证过 vLLM SSE 流式;浏览器侧 React/Vue 消费、EventSource、buffer 拼半截 JSON 我都没写过。" |

**安全答法模板:** 理论定位 → 引用 lessons/0004/0007/0015 的相邻锚点 → 显式承认未实践。

---

## P2 · 视野题(一句话带过,不深挖)

| 缺口 | 影响题数 | 一句话 |
|---|---|---|
| **设计 ChatGPT(系统设计宏观题)** | T06 q0598、q1069 | "我会按'无状态 API 网关 → 调度器 → GPU 池 + 自动扩缩 → KV 缓存池 + prefix 路由 → 监控告警'分层,但没设计过生产级。" |
| **端侧 AI(手机 NPU/DSP/高低配)** | T08 q4131 | "端侧没碰过;常识是按机型分级路由 + 模型蒸馏 + NPU 加速;推理功耗监控指标我会用类似 P99 latency + token/J。" |
| **Token 成本估算(月账单)** | T10 q1995 | "DeepSeek 公开定价 × 平均调用 token × DAU × 月活天数 = 估算量级;前缀缓存能省 30-60%(基于我的 A/B 命中率)。" |
| **多 LLM 实例 prefix 路由策略** | T10 q1962 | "按 hash(用户 ID) 路由到固定实例,保证同一会话 prefix 复用命中;一致性哈希 + sticky session 是经典方案。" |
| **租户隔离(prefix cache 工程实现)** | T10 q3705 | "per-tenant prefix tree + block 引用计数 + 显存上限;我没写过。" |
| **LC91 Decode Ways(LeetCode 手撕题)** | T09 q2519 | **跳过,不写卡片**(tags=综合软素质,与推理主题不符,本项目不挂锚点)。 |

---

## 跳过题汇总(1 题)

| qid | 原因 | 主题 |
|---|---|---|
| q2519 | LC91 Decode Ways 是 LeetCode 手撕题,与 llm-serving-ops-study 推理主题不符 | T09 prefill/decode/batch |

---

## 缺口统计

- **总题数:** 105 张卡片(命中题库 106 题,1 题 q2519 因 LeetCode 手撕跳过)
- **P0 缺口题:** 约 17 题(T07 全部 + T11 吞吐类 + T09 PD 分离 + T03/T10 部分)→ 补 Phase 4 + PD demo 可大幅升级
- **P1 缺口题:** 约 30 题(理论答,诚实标注即可)
- **P2 视野题:** 约 6 题(一句话带过)
- **零缺口(强锚点覆盖):** 约 52 题(项目实测 + 数据验证)

---

## 行动建议

1. **下次学习:** 优先补 Phase 4(压测阶梯)+ PD 分离 demo,把 P0 缺口题升级到"实测证据"
2. **面试准备:** P1 题用诚实答法模板(理论 + 相邻锚点 + 承认未实践);P2 题用一句话带过
3. **诚实底线:** 永远不编造任何实测数字,所有"我的项目里"必须挂锚点
